# Dython
A Python interpreter that simulates Pony's Orca GC — actor concurrency, reference capabilities (`iso`, `val`, `ref`), and per‑actor independent garbage collection, all in a single readable file.

